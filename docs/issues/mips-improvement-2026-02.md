# MIPS ページ 改善完了記録（Issue #32 用コメント）

以下の内容を Issue #32 にコメントとして追記してください。

---

```
2026-02-07: 改善指摘メモに基づく改修完了

## 実施内容

### 1. コード例のデザイン意図
- 黒いターミナル風画面の意図がわからない → 説明を追加
- 「プログラミングの画面をイメージした表示。数値や式を画面に出る形で見せる」と文言追加

### 2. ポイント・わかったことの枠テンプレート化
- ポイント、わかったこと、覚え方、試験ヒントを `.key-point-box` で統一
- globals.css に枠スタイル追加（左ボーダー #14b8a6、背景 #f0fdfa）

### 3. マイクロ秒・計算の説明整理
- 「1かけるMIPS」「1割る〜」の混乱解消 → 計算式をシンプルに「1 ÷ MIPS値 = 平均命令実行時間（μs）」
- 「平均命令実行時間分の1秒 × 10のマイナス6乗」との関係を追記

### 4. 計算デモの使い方
- 何をどう使うか不明 → 「MIPSと平均命令実行時間を相互に変換。どちらかを入力するともう一方が自動更新」と明示
- デモ内にも短い説明を追加

### 5. 単位階段デモのUX
- 「上下」の書き方 → 「×1000」「÷1000」「リセット」に変更
- 「現在地」ボタン → 黄色い丸（12px）のみの表示に変更

### 6. 全体のまとまり・FAQ
- まとめを3つの柱に整理（①MIPSとは ②平均命令実行時間との関係 ③なぜマイクロ秒か）
- FAQと本文の重複を解消 → MIPSとは・マイクロ秒・10⁻⁶の説明は本文のみに。FAQは「MIPSと性能」「試験対策」の2項目に絞る

## 変更ファイル
- app/terms/mips/page.tsx
- app/globals.css（.key-point-box 追加）
```

---
